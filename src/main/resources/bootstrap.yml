spring:
  cloud:
    config:
      enabled: true
      name: patient-mangt-service
      #uri: "http://localhost:8888"
  application:    name: patient-mangt-service
  config:    import:    - configserver:http://localhost:8888